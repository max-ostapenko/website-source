{
  "name": "website-source",
  "version": "1.0.0",
  "description": "Source code for maxostapenko.com",
  "private": true,
  "scripts": {
    "clean": "rm -rf _site",
    "render": "npm run clean && quarto render src/",
    "preview": "quarto preview src/",
    "deploy": "firebase deploy --only hosting --project max-ostapenko",
    "deploy-preview": "firebase hosting:channel:deploy --project max-ostapenko --expires 2d",
    "emulate": "firebase emulators:start"
  },
  "devDependencies": {
    "firebase-tools": "15.5.1"
  },
  "engines": {
    "node": ">=24.0.0"
  }
}
