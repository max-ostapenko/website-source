---
layout: layouts/main.njk
---

Readily available public datasets in Google Analytics Hub.

{% set datasets = search.pages("type=dataset") %}

{% for dataset in datasets %}
<li>
    <article id="{{ dataset.data.date }}">
        <header class="mb2">
            <h1 class="f4 no-rhythm mb0">
                <a href="#{{ dataset.data.date }}" title="Permalink" class="font-serif no-underline target">{{ dataset.data.date }} <span>#</span></a>
            </h1>
        {% if dataset.data.location %}<p title="Posted from" class="muted font-sans mb0 f5 target">{{ dataset.data.location }}</p>{% endif %}
        </header>

        <div class="prose">
        <a class="PostItem__Link" href="{{ post.data.url | url }}" title="About {{ post }} reading time">{{ post.data.title }}</a>
        </div>
    </article>
    </li>
{% endfor %}
